apply plugin: 'android'

dependencies {
    compile files('libs/android-support-v4.jar')
    compile project(":AmarinoLibrary")
}

android {
    compileSdkVersion 17
    buildToolsVersion "17.0.0"

    defaultConfig {
        minSdkVersion 7
        targetSdkVersion 16
    }
}
